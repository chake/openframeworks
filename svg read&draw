1.  openframeworks 有一个很好的小工具projectGenerator，可以根据我们的目的帮助我们生成一个关联好头文件的工程
 
2.	在利用这个工具生成项目时addon直接选择加入"ofxsvg"
  这样会生成一个已经关联好了对svg文件进行操作所需要的所有头文件，并且它自动生成了一个完整的框架。我们只需要填充部分函数就可以直接使用。

3.	我们只需要调用load以及draw函数就可以直接读入&绘图。非常方便。
 
 
C里面可以直接修改对象的任意一个变量，所以细节修改均是可行的。

